# Adaption-for-BERT
This repository presents an adaptation of the BERT-base-cased model for sentiment analysis on the IMDB dataset. Two parameter-efficient fine-tuning methods, LoRA (Low-Rank Adaptation) and LoRA + (Low Rank Adaptation of Large Models), are evaluated and compared against full fine-tuning of BERT-base-cased.
